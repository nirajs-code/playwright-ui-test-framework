{
  "name": "playwright-ui-test-framework",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "npx playwright test",
    "test:api": "npx playwright test tests/api/get.posts.spec.ts",
    "show-report": "npx playwright show-report",
    "test-ui": "npx playwright test --ui",
    "test-ui-debug": "npx playwright test --ui --debug",
    "codegen": "npx playwright codegen demo.playwright.dev/todomvc",
    "report:allure": "allure generate allure-results --clean -o allure-report && allure open allure-report",
    "test:smoke": "npx playwright test --grep @api"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@faker-js/faker": "^9.9.0",
    "@playwright/test": "^1.60.0",
    "@types/node": "^25.9.0",
    "allure-commandline": "^2.42.0",
    "allure-playwright": "^3.9.0"
  },
  "dependencies": {
    "dotenv": "^17.4.2"
  }
}
